# Handover: zenbones-mono - 2026-03-26 16:05

## Current Task
Add Nerd Font patching for Zenbones Brainy to the release workflow.

## Status
- [x] Added `nerd-font` job to `.github/workflows/release.yml`
- [ ] Uncommitted — needs review, commit, and push
- Current branch: `main`
- Last commit: `95415be - feat: upgrade iosevka to 34.2.1`

## Immediate Next Steps
1. Review the workflow change in `.github/workflows/release.yml`
2. Commit and push
3. Tag a release to trigger the workflow and verify it works end-to-end
4. Install the patched Nerd Font locally once the release artifacts are available

## Blockers / Open Questions
- The `font-patcher` flags (`--complete --careful`) should work but haven't been tested in CI yet — may need tweaking if glyphs collide or the patcher version changes its CLI
- Only patching Zenbones-Brainy currently — user may want other variants patched too

## Key Decisions Made
- Sparse-clone nerd-fonts repo (only `font-patcher` + `src/glyphs`) to avoid downloading the full 4GB+ repo
- Used `--complete` flag to include all Nerd Font icon sets
- Used `--careful` flag to avoid overwriting existing Iosevka glyphs
- Only patching TTF (hinted) variant, not unhinted/WOFF2
- Added as a separate CI job (`nerd-font`) that depends on the `build` job, keeping the existing build matrix untouched
- Release job now depends on both `build` and `nerd-font`

## Context the Next Session Needs
- The only changed file is `.github/workflows/release.yml` — the `nerd-font` job at line 68
- Inspiration came from `damian-elenbaas/zenbones-brainy-nerdfont` which does this manually
- The user's fork is at `johan--/zenbones-mono` (origin remote)
- Font build uses Iosevka build system — `private-build-plans.toml` defines all 5 variants
- Iosevka version pinned in `.iosevka-version` (currently `v34.2.1`)
